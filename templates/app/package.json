{
  "name": "{{PACKAGE_NAME}}",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@babel/core": "^7.28.4",
    "@babel/preset-typescript": "^7.27.1",
    "@rollup/plugin-babel": "^6.0.4",
    "@solid-native/core": "^0.0.1",
    "solid-js": "^1.8.0"
  },
  "devDependencies": {
    "@babel/preset-typescript": "^7.24.7",
    "@rollup/plugin-babel": "^6.0.4",
    "@rollup/plugin-commonjs": "^25.0.7",
    "@rollup/plugin-node-resolve": "^15.3.0",
    "@rollup/plugin-alias": "^5.1.0",
    "babel-preset-solid": "^1.8.0",
    "rollup": "^4.21.0",
    "typescript": "^5.6.2"
  },
  "scripts": {
    "build": "rollup -c",
    "prebuild:ios": "node ../../scripts/prebuild-ios.js",
    "ios:pods": "cd ios && pod install",
    "ios:build": "node -e \"const app=require('./app.json'); const name=app.expo.name.replace(/[^a-zA-Z0-9]/g, ''); const workspace=require('fs').readdirSync('ios').find(f=>f.endsWith('.xcworkspace')); console.log('Building', name, 'workspace:', workspace); process.exit(0)\" && xcodebuild -workspace ios/*.xcworkspace -scheme {{XCODE_SCHEME}} -configuration Debug -sdk iphonesimulator -derivedDataPath ios/.build",
    "ios:run": "xcrun simctl bootstatus booted -b && xcrun simctl install booted ios/.build/Build/Products/Debug-iphonesimulator/{{XCODE_SCHEME}}.app && xcrun simctl launch booted {{BUNDLE_ID}}",
    "ios:logs": "xcrun simctl spawn booted log stream --style compact --level debug --predicate 'process == \"{{XCODE_SCHEME}}\" AND (eventMessage BEGINSWITH \"[SN]\" OR eventMessage CONTAINS \"JS Error\")'",
    "ios:dev": "(yarn ios:logs &) && yarn build && yarn ios:build && yarn ios:run"
  }
}
