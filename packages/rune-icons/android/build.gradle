apply plugin: 'com.android.library'

android {
    namespace "dev.rune.icons"
    compileSdkVersion 34

    defaultConfig {
        minSdkVersion 21
    }

    sourceSets {
        main {
            assets.srcDirs = ['src/main/assets']
        }
    }
}
