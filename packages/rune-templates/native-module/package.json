{
  "name": "@rune/{{MODULE_NAME}}",
  "version": "0.0.1",
  "description": "{{MODULE_DESCRIPTION}}",
  "type": "module",
  "main": "dist/esm/index.js",
  "module": "dist/esm/index.js",
  "types": "dist/types/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/esm/index.js",
      "types": "./dist/types/index.d.ts"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist",
    "ios",
    "android",
    "Rune{{MODULE_NAME_PASCAL}}.podspec"
  ],
  "scripts": {
    "build": "yarn build:js && yarn build:types",
    "build:js": "rm -rf dist/esm && tsc --project tsconfig.json",
    "build:types": "tsc --project tsconfig.types.json",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "rune",
    "{{MODULE_NAME}}",
    "native-module"
  ],
  "license": "MIT",
  "peerDependencies": {
    "@rune/core": ">=0.0.1",
    "@rune/components": ">=0.0.1",
    "solid-js": ">=1.8.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "runeNative": {
    "ios": {
      "pods": [
        {
          "name": "Rune{{MODULE_NAME_PASCAL}}",
          "podspec": "./Rune{{MODULE_NAME_PASCAL}}.podspec"
        }
      ],
      "initializer": {
        "className": "{{MODULE_NAME_PASCAL}}Module",
        "method": "initializeWith:"
      }
    },
    "android": {
      "modules": [
        {
          "name": "Rune{{MODULE_NAME_PASCAL}}",
          "path": "./android/Rune{{MODULE_NAME_PASCAL}}"
        }
      ],
      "initializer": {
        "className": "{{MODULE_NAME_PASCAL}}",
        "method": "initialize",
        "package": "dev.rune.{{MODULE_NAME}}"
      }
    }
  }
}
