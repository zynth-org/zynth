# Rune Bottom Sheet (iOS)

This directory will host the iOS native implementation for `rune-bottom-sheet` (UIView, descriptor, etc.).

Add your Swift/Objective-C files under `ios/src` and include any supporting metadata once the component is ready.
